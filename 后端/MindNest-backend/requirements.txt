fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
pymysql==1.1.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
opencv-python==4.10.0.84
python-dotenv==1.0.1
openai==1.82.0
python-docx==1.1.2
reportlab==4.2.5

# Optional real OCR engine. Install manually if your environment supports it:
# Windows CPU stable combination tested for this project:
# paddleocr==2.10.0
# paddlepaddle==2.6.2
# numpy==1.26.4
# albumentations==1.3.1
# scikit-image
# qudida
# scikit-learn
# joblib
# threadpoolctl

# Optional YOLO / YOLOE detector. Required for backend focus frame object detection:
# pip install -U ultralytics
# pip install "git+https://github.com/ultralytics/CLIP.git"

# Optional formula recognition engine. The app falls back to heuristic formula
# extraction when Pix2Text is not installed:
# pix2text
